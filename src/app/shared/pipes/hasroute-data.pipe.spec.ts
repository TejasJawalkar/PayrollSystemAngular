import { HasrouteDataPipe } from './hasroute-data.pipe';

describe('HasrouteDataPipe', () => {
  it('create an instance', () => {
    const pipe = new HasrouteDataPipe();
    expect(pipe).toBeTruthy();
  });
});
